type Score
  @model
  @auth(rules: [
    { allow: public, provider: apiKey, operations: [read] }
    { allow: private, provider: userPools, operations: [create] }
  ]) {
  id: ID!
  userId: String!
  username: String!
  score: Int!
  timestamp: AWSDateTime!
}

type Mutation {
  submitScore(score: Int!): Score @aws_cognito_user_pools
}
